jQuery(document).ready(function(){
  jQuery('.bdnews_bangla').bxSlider({
	  auto: true,
	  adaptiveHeight: true,
	  pager: true,
	  controls: false,
	  infiniteLoop: true,
	  speed: 500,
	  mode: 'horizontal',
	  pause: 10000,
	  ticker: false,
	  pagerType: 'full',
	  randomStart: true
	  });
});